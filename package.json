{
  "name": "ucilica",
  "version": "1.0.0",
  "private": true,
  "description": "Učilica - Interaktivna aplikacija za učenje",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently -k -n BE,FE -c blue,green \"cd backend && npm run dev\" \"wait-on http://localhost:3000/api/subjects && cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "seed": "cd backend && npm run seed",
    "seed:2": "cd backend && npm run seed:2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
